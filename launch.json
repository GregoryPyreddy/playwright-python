{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: pytest",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/tests/step_defs",
            "args": [
                "--no-cov",
                "--capture=no"
            ],
            "console": "integratedTerminal",
            "justMyCode": false
        }
    ]
}